/*
Copyright 2013 Sandia Corporation.
Under the terms of Contract DE-AC04-94AL85000 with Sandia Corporation,
the U.S. Government retains certain rights in this software.
Distributed under the BSD-3 license. See the file LICENSE for details.
*/

package gov.sandia.n2a.exporters;


public class LEMSExporterTest {
/*
    @Test
    public void test() throws IOException
    {
        String name = "testPart";
        String owner = "owner";
        String notes = "notes";
        PartXType type = PartXType.COMPARTMENT;
        Boolean devTest = true;
        PartX parent = null;
        PartX testPart = new PartX(name, owner, notes, type, devTest, parent);
        List<EquationX> eqs = new ArrayList<EquationX>();
        eqs.add(new EquationX("a=5",0));
        eqs.add(new EquationX("x'=a",1));
        testPart.setEqs(eqs);
        testPart.setPartAssociations(new ArrayList<PartAssociation>());
        LEMSExporter exporter = new LEMSExporter();
        File file = new File("lemsExport.xml");
        exporter.export(testPart, file);
        assert(file.exists());
    }*/
}
