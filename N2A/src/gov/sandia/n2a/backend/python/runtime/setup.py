from setuptools import setup

setup(
    name='n2a',
    version='1.3',
    description='Support routines for accessing N2A files.',
    install_requires = [],  # We rely exclusively on Python standard libraries.
    packages=['n2a']
    )
